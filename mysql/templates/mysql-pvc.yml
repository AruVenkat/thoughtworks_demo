apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
  namespace: mediawiki-ns
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: mediawiki-sc
  resources:
    requests:
      storage: 4Gi

